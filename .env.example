# Example environment variables for MegaMind
# Copy this file to `.env` and fill in real values. Do NOT commit secrets.

# General
ENV=development
SERVER_PORT=3000

# LLM configuration (provider-agnostic)
# - Set LLM_PROVIDER to 'openai' or 'gemini'
# - Prefer setting `LLM_API_KEY`. If not provided, `OPENAI_API_KEY` is used as a fallback for OpenAI.
LLM_PROVIDER=openai
LLM_API_KEY=
# Optional OpenAI fallback (kept for backwards compatibility)
OPENAI_API_KEY=

# Gemini / Google (only required if using Gemini provider)
# - Either set GOOGLE_API_KEY (API key) or configure Application Default Credentials
#   via GOOGLE_APPLICATION_CREDENTIALS pointing to a service account JSON key.
GOOGLE_API_KEY=
GOOGLE_PROJECT_ID=
GOOGLE_LOCATION=us-central1
GEMINI_EMBEDDING_MODEL=textembedding-gecko
GOOGLE_APPLICATION_CREDENTIALS=

# Embedding dimension used when creating Qdrant collections
# Set this to the vector size of your model (e.g., OpenAI ada/002 = 1536)
EMBEDDING_VECTOR_SIZE=1536

# Qdrant
QDRANT_HOST=
QDRANT_KEY=

# Database
DATABASE_URL=

# Optional integrations
TELEGRAM_CHAT_ID=
TELEGRAM_BOT_TOKEN=
ALARMS_WHOOK_URL=

# App tuning
MAX_CONCURRENCY=2
CHROME_PATH_ON_HOST=/usr/bin/google-chrome-stable

# Scraper triggers
TRIGGER_SCRAPPER_URL=
TRIGGER_SCRAPPER_STATUS=

# Notes:
# - After updating `.env`, restart the app so environment variables are reloaded.
# - For Gemini (Vertex AI) embedding support you may prefer to install
#   `@google-cloud/aiplatform` and use ADC (service account) rather than API keys.
# - If your embeddings model uses a different dimension, set EMBEDDING_VECTOR_SIZE
#   so Qdrant collections are created with the correct vector size.
